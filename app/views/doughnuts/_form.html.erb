<%= form_for @doughnut, html: { multipart: true } do |f| %>
	<% if @doughnut.errors.any? %>
	<h2><%= pluralize(@doughnut.errors.count, "error") %> prevented this doughnut from saving</h2>
		<ul>
			<% @doughnut.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<% end %>	
		</ul>
	<% end %>
	<%= f.label :title %> <br>
	<%= f.text_field :title %>

	<br>
	<br>

	<%= f.label :description, "Describe this doughnut" %> <br>
	<%= f.text_area :description %>
	
	<br>
	<br>

	<%= f.label :image %> <br>
	<%= f.file_field :image %>
	
	<br>
	<br>

	<%= f.submit %>

<% end %>